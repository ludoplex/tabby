interface:
  display_name: "Tabby"
  short_description: "Repo-specific patterns and workflows for tabby"
  default_prompt: "Use the tabby repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true